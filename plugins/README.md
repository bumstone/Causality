# Experimental plugin metadata

`causality-workflows/manifest.json` is retained as an experimental host-plugin
descriptor. It is not packaged with the Python runtime and is not a supported
runtime manifest. The canonical workflow definitions remain
`src/causality/workflows.py` until an external consumer contract is confirmed.
